# Favlist：自托管 Web 应用实施计划

## 1. 产品与架构

- 构建单用户、自托管 Web 应用：React + TypeScript 前端、FastAPI 后端、SQLite 持久化。
- Docker Compose 包含 `web`、`api`；数据挂载到持久卷，默认仅绑定本机端口。
- Compose 提供可选 `caddy` profile，通过 `APP_DOMAIN` 启用公网 HTTPS。
- 管理员用户名、Argon2 密码哈希和会话密钥通过环境变量配置；无注册和多用户系统。
- 使用签名 HttpOnly、SameSite 会话 Cookie；登录失败按 IP 限流，数据和封面接口均需登录。

## 2. 数据、后台任务与缓存

- `items` 保存编号、标题、简介、作者、页数、日期、观看/点赞/评论数、状态、错误、添加及刷新时间；标签使用关联表保存名称和外部服务顺序。
- 状态为 `pending / loading / ready / error / refreshing`；服务重启后重新排队未完成任务。
- 导入接口接受文本，识别大小写不敏感的 `ID123` 和纯数字，支持空格、换行、逗号及分号分隔。
- 单次最多导入 5000 个编号；导入时规范化、去重并立即写入 SQLite。重复项跳过且不刷新，返回新增、重复和无效统计。
- 使用进程内异步队列获取详情和封面，默认并发 4、超时 20 秒、最多重试 3 次。
- 手动刷新绕过缓存；刷新失败时保留最后一次成功数据并记录错误。
- 元数据以 SQLite 为持久缓存，只在首次导入或手动刷新时请求外部服务。
- 外部服务短期缓存采用进程内 TTL/LRU：默认 15 分钟、最多 512 项；相同编号使用 single-flight 防止重复请求。
- 封面统一转换为 WebP，原子写入 `/data/covers/{id}.webp`：
  - 对仍存在的条目不做容量淘汰。
  - 删除条目时删除对应封面。
  - 启动时及每 24 小时清理孤儿文件。
  - 文件缺失时按需重新下载。
  - 使用 ETag、私有缓存头和 `cover_version`，刷新后自动 cache bust。
- 不提供缓存管理页面；后台自动维护全部缓存。

## 3. 标签分类与视觉规则

配置文件使用两个有序列表：

```yaml
ui:
  disliked_tags:
    - 不喜欢的标签
  liked_tags:
    - 喜欢的标签
```

- 标签名称经过空白清理和 Unicode 规范化后进行精确匹配。
- 同一标签不能同时属于两组；检测到冲突时服务启动失败，并明确列出冲突标签。
- 后端为每个标签返回 `emphasis: disliked | liked | normal`，保证列表、详情和筛选器采用同一规则。
- 屏幕空间不足时，显示顺序固定为：
  1. 不喜欢标签，按 YAML 顺序。
  2. 喜欢标签，按 YAML 顺序。
  3. 普通标签，按外部服务顺序。
- 桌面列表最多显示 6 个标签，手机最多显示 2 个，其余显示 `+N`；详情抽屉显示全部标签。
- 不喜欢标签：
  - 使用明显的红色背景、红色边框和警告图标。
  - 明暗主题分别使用符合对比度要求的红色色阶。
- 喜欢标签：
  - 使用常驻、慢速流动的彩虹描边“跑马灯”。
  - 标签内部保持稳定的高对比背景和文字，动画只作用于边框，不使用彩虹文字。
  - 动画周期约 8 秒，避免快速闪烁。
  - 当系统启用 `prefers-reduced-motion` 时自动停用动画，改为静态彩虹渐变边框。
- 普通标签使用低强调度的中性色胶囊。

## 4. 前端体验与接口

- 登录后进入紧凑横向列表；默认每页 50 条、最近添加优先，可切换标题或编号排序。
- 桌面行包含：多选框、3:4 封面、ID 编号、标题、作者、标签、状态和操作。
- 手机仍为横向条目，但隐藏作者等次要字段；保留封面、编号、标题、重点标签和操作菜单。
- 支持按编号、标题、作者搜索；标签多选采用 AND 语义。
- 导入弹窗只提供文本框；提交后显示新增、重复和无效统计，并轮询后台状态。
- 支持逐条及多选批量刷新、删除；删除前二次确认。
- 点击条目打开站内详情抽屉，显示大封面、完整标签和元数据，并提供外部服务外链。
- 明暗主题首次跟随系统，用户选择保存在浏览器。
- 导出功能生成 UTF-8 文本，每行一个 `ID{id}`，包含全部条目。

主要接口：

- `POST /api/auth/login`、`POST /api/auth/logout`、`GET /api/auth/me`
- `POST /api/import`
- `GET /api/items`：分页、搜索、标签筛选和排序
- `GET /api/items/{id}`、`GET /api/items/{id}/cover`
- `POST /api/items/{id}/refresh`
- `POST /api/items/bulk-refresh`
- `DELETE /api/items`
- `GET /api/tags`：返回标签、数量和强调类型
- `GET /api/export/ids`

## 5. 测试与验收

- 单元测试覆盖编号解析、去重、标签分类、冲突检测、标签显示优先级、AND 筛选、缓存 TTL/LRU、single-flight 和孤儿封面清理。
- 后端集成测试使用模拟外部服务客户端，验证导入、重试、刷新保留旧数据、批量删除、按需恢复封面及认证保护。
- 前端测试覆盖导入汇总、加载/失败状态、批量操作、详情抽屉、主题持久化和 `+N` 折叠。
- 视觉测试覆盖：
  - 不喜欢标签在明暗主题下均明显标红。
  - 喜欢标签显示慢速彩虹边框。
  - 减少动画模式下彩虹边框保持静态。
  - 两类重点标签同时存在时，不喜欢标签优先占用有限空间。
  - 手机宽度下仍保留横向条目和重点标签。
- 端到端验收：
  - 导入后逐步显示封面、名称和标签。
  - 刷新页面、重启容器后清单及封面仍存在。
  - 重复导入不创建记录或发起外部服务请求。
  - 搜索、标签筛选、刷新、删除及导出可用。
  - 删除封面文件后再次访问可按需恢复。
  - 未登录用户不能读取列表、详情或封面。
  - 默认 Compose 可本机运行，启用 Caddy profile 后可通过 HTTPS 访问。

## 6. 明确不包含

- 不下载内容或条目正文。
- 不提供公开注册、多用户隔离、评分、备注、分组或社交功能。
- 不自动周期性刷新条目元数据。
- 不提供 TXT/CSV 文件上传。
- 不提供缓存管理 UI。

<!-- Documentation header: product architecture, behavior, and acceptance plan. -->
# Favlist：自托管收藏库实施计划

## 1. 产品与架构

- 构建单用户、自托管的收藏整理应用：React + TypeScript 前端、FastAPI 后端、SQLite 持久化。
- Docker Compose 包含 `web` 和 `api`；数据挂载到持久卷，默认仅绑定本机端口。
- Compose 提供可选 `caddy` profile，通过 `APP_DOMAIN` 启用公网 HTTPS。
- 管理员用户名、Argon2 密码哈希和会话密钥通过环境变量配置；无注册和多用户系统。
- 使用签名 HttpOnly、SameSite 会话 Cookie；登录失败按 IP 限流，数据和封面接口均需登录。

## 2. 数据、后台任务与缓存

- 条目保存编号、标题、简介、作者、页数、日期、互动统计、状态、错误、添加及刷新时间；标签使用关联表保存名称和顺序。
- 状态为 `pending / loading / ready / error / refreshing`；服务重启后重新排队未完成任务。
- 导入接口接受数字编号，支持空格、换行、逗号及分号分隔。
- 单次最多导入 5000 个编号；导入时规范化、去重并立即写入 SQLite。
- 使用进程内异步队列补全详情和封面，失败时保留最后一次成功数据并记录错误。
- 短期缓存采用进程内 TTL/LRU，相同编号使用 single-flight 防止重复请求。
- 封面统一转换为 WebP 并原子写入；删除条目时删除对应封面，并定期清理孤儿文件。

## 3. 标签分类与视觉规则

- 配置文件使用 `disliked_tags` 和 `liked_tags` 两个有序列表。
- 标签名称经过空白清理和 Unicode 规范化后精确匹配；同一标签不能同时属于两组。
- 列表按不喜欢、喜欢、普通的顺序显示，屏幕空间不足时折叠其余标签。
- 不喜欢标签使用明显警示样式；喜欢标签使用高对比渐变边框，并遵循 `prefers-reduced-motion`。

## 4. 前端体验

- 登录后进入紧凑横向列表；默认最近添加优先，可切换标题或编号排序。
- 支持按编号、标题、作者搜索；标签多选采用 AND 语义。
- 导入弹窗显示新增、重复和无效统计，并轮询后台处理状态。
- 支持逐条及批量刷新、删除和编号导出。
- 点击条目打开站内详情抽屉，显示封面、完整标签和元数据。
- 明暗主题首次跟随系统，用户选择保存在浏览器。
- 默认隐私模式不渲染封面、标题、作者或标签，页面进入后台后自动隐藏。

## 5. 测试与验收

- 单元测试覆盖编号解析、去重、标签分类、缓存、single-flight 和孤儿封面清理。
- 后端集成测试使用模拟适配器，验证导入、重试、刷新、批量删除、封面恢复及认证保护。
- 前端测试覆盖导入汇总、加载/失败状态、批量操作、详情抽屉、主题持久化和标签折叠。
- 未登录用户不能读取列表、详情或封面。
- 默认 Compose 可本机运行，启用 Caddy profile 后可通过 HTTPS 访问。

## 6. 明确不包含

- 不提供公开注册、多用户隔离、评分、备注、分组或社交功能。
- 不自动周期性刷新条目元数据。
- 不提供 TXT/CSV 文件上传或缓存管理界面。
